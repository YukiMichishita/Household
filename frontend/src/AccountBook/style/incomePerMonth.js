import React, { useState, useEffect } from 'react';
import { withStyles, makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  selectMonthText: {
    category: {
      textDecoration: 'underline',
    },
  },

  addCircleIcon: { position: 'relative' },
});
